program escaleno;

var
    a, b, c: longint;
    p, calc, calcfinal: real;
    
begin

    read(a);
    read(b);
    read(c);
    
    p:= (a+b+c) / 2;
    calc:= p * ((p-a)*(p-b)*(p-c));
    calcfinal:= sqrt(calc);
    
    writeln(calcfinal:0:3);
    
end.
